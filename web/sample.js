class Avatar {   
  constructor(id, name) {
    this.id = id;
    this.name = name;
  } 
  jump;   left;   right;   stop; 
}

var avatar=Avatar();

// using Avatar class write a code to call the jump method of the avatar object 